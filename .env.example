# MySQL connection string (replace USER, PASSWORD, DATABASE)
DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/DATABASE"

# Frontend origin (for CORS)
FRONTEND_URL=http://localhost:3000

# Port the backend should run on
PORT=5000
